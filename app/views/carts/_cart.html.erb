<%= link_to "#{current_order.order_items.size} items with a value of #{current_order.subtotal }$", cart_path%>

<!-- Replaces 'cart_path' by 'carts_show_path'
<%= link_to "#{current_order.order_items.size} items with a value of #{current_order.subtotal }$", carts_show_path%>
-->